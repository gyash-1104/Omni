# Admin UI Environment Variables
# Copy to .env.local for local dev (do NOT commit .env.local)

# Backend API URL
# Local dev: leave empty (uses Vite proxy to localhost:8000)
# Production (Vercel): leave empty — vercel.json proxies /admin → Render (no CORS needed)
# VITE_API_URL=
